# compass skill

Install via repo `install.sh` / `scripts/install.ps1`, or copy this folder to
`~/.cursor/skills/compass` or workspace `.cursor/skills/compass`.

See [SKILL.md](SKILL.md) for slash commands and evidence gate rules.
