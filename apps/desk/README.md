# Compass Desk

Lightweight local workbench embedded in `compass_core.desk`.

```bash
python -m compass_core.cli desk --root ../../content --port 8765
```

Or from repo root after install:

```bash
python -m compass_core.cli desk
```

No Node build required — stdlib HTTP server serves a single-page overview of jobs, track, diagnoses, and evidence.
